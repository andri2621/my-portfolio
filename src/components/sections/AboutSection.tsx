const AboutSection = () => {
  return (
    <section
      id='about'
      className='flex min-h-screen items-center justify-center'
    >
      AboutSection
    </section>
  );
};

export default AboutSection;
